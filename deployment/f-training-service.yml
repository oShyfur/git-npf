apiVersion: v1
kind: Service
metadata:
  name: $K8S_SERVICE_NAME
  namespace: $NAMESPACE
spec:
  ports:
  - nodePort: $K8S_NODE_PORT
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: np-frontend
  type: NodePort
