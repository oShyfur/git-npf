apiVersion: v1
kind: Service
metadata:
  name: np-frontend
  namespace: sylhet
spec:
  ports:
  - nodePort: 30800
    name: npf-fr-sy-port
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: np-frontend
  type: NodePort
