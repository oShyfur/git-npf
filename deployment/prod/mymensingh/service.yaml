apiVersion: v1
kind: Service
metadata:
  name: np-frontend
  namespace: mymensingh
spec:
  ports:
  - nodePort: 30200
    name: npf-fr-my-port
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: np-frontend
  type: NodePort
